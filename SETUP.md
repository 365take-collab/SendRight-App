# セットアップ手順（初心者向け）

## ✅ ステップ1: 依存関係のインストール（完了済み）

既に完了しています！

## 🔑 ステップ2: AI APIキーの取得（無料のGroq API推奨）

### オプションA: Groq API（無料・推奨）⭐

**Groq APIは無料プランがあり、クレジットカード不要で使えます！**

1. **Groqのアカウント作成・ログイン**
   - https://console.groq.com/ にアクセス
   - アカウントがない場合は作成（メールアドレスで登録、クレジットカード不要）

2. **APIキーの取得**
   - ログイン後、左メニューの「API Keys」を選択
   - 「Create API Key」をクリック
   - キー名を入力（例: "LINE-DM-Assistant"）
   - 生成されたキーをコピー（⚠️ このキーは一度しか表示されません！）

3. **無料プランの制限**
   - レートリミットあり（1分あたりのリクエスト数など）
   - クレジットカード不要
   - 無料で利用可能

### オプションB: OpenAI API（有料）

**OpenAI APIは使用量に応じて課金されます（GPT-4o-miniは比較的安価）**

1. **OpenAIのアカウント作成・ログイン**
   - https://platform.openai.com/ にアクセス
   - アカウントがない場合は作成（メールアドレスで登録）

2. **APIキーの取得**
   - ログイン後、右上のプロフィールアイコンをクリック
   - 「API keys」を選択
   - 「Create new secret key」をクリック
   - キー名を入力（例: "LINE-DM-Assistant"）
   - 生成されたキーをコピー（⚠️ このキーは一度しか表示されません！）

3. **クレジットの追加**
   - 左メニューの「Billing」を選択
   - 「Add payment method」でクレジットカードを登録
   - 最低$5程度のクレジットを追加（使用量に応じて課金されます）

## 🔐 ステップ3: 環境変数ファイルの作成

プロジェクトフォルダ内に`.env.local`ファイルを作成します。

### 方法1: ターミナルで作成（推奨）

ターミナルで以下のコマンドを実行：

```bash
cd /Users/kawamuratakeshi/Cursor/sendright
cat > .env.local << 'EOF'
# Groq API（完全無料・推奨）
GROQ_API_KEY=ここに取得したGroq APIキーを貼り付け

# OpenAI APIは設定しない（設定すると有料になる）
# OPENAI_API_KEY=ここに取得したOpenAI APIキーを貼り付け  ← コメントアウト

JWT_SECRET=ランダムな文字列（例: my_secret_key_12345_change_in_production）
EOF
```

### 方法2: テキストエディタで作成

1. テキストエディタ（メモ帳、VS Codeなど）を開く
2. 以下の内容を入力：

```
# Groq API（完全無料・推奨）
GROQ_API_KEY=ここに取得したGroq APIキーを貼り付け

# OpenAI APIは設定しない（設定すると有料になる）
# OPENAI_API_KEY=ここに取得したOpenAI APIキーを貼り付け  ← コメントアウト

JWT_SECRET=ランダムな文字列（例: my_secret_key_12345_change_in_production）
```

3. ファイル名を`.env.local`として保存
4. 保存場所: `/Users/kawamuratakeshi/Cursor/sendright/.env.local`

⚠️ **重要**: 
- **完全無料で使う場合（推奨）**: `GROQ_API_KEY=`の後に、Groq APIキーを貼り付けてください
- **`OPENAI_API_KEY`は設定しないでください**（設定すると有料になります）
- `JWT_SECRET=`の後は、適当なランダムな文字列でOKです（例: `my_secret_key_12345`）

**💡 完全無料で使う方法**: 
- Groq APIキーのみを設定すれば、**完全無料**で利用できます
- クレジットカード不要
- レートリミットあり（制限を超えた場合は、しばらく待ってから再試行してください）

## 🚀 ステップ4: 開発サーバーの起動

ターミナルで以下のコマンドを実行：

```bash
cd /Users/kawamuratakeshi/Cursor/sendright
npm run dev
```

成功すると、以下のようなメッセージが表示されます：

```
  ▲ Next.js 14.x.x
  - Local:        http://localhost:3000
```

## 🌐 ステップ5: ブラウザでアクセス

ブラウザで以下のURLを開いてください：

**http://localhost:3000**

## 📝 使い方

1. **新規登録**
   - ブラウザで http://localhost:3000/login を開く
   - メールアドレスとパスワードを入力して登録

2. **会員登録（サブスクリプション）**
   - ログイン後、会員登録ページに移動
   - 月額または年額プランを選択（現在はデモ版）

3. **返信生成**
   - ホームページで女性からのメッセージを入力（またはスクリーンショットをアップロード）
   - 「返信を生成」ボタンをクリック
   - AIが即座に返信を提案します！

## ❓ トラブルシューティング

### エラー: "OPENAI_API_KEY is not defined"
→ `.env.local`ファイルが正しく作成されているか確認してください

### エラー: "APIキーを確認してください"
→ OpenAI APIキーが正しいか、クレジットが残っているか確認してください

### ポート3000が既に使用されている
→ 別のアプリが動いている可能性があります。そのアプリを終了するか、Next.jsの設定で別のポートを使用してください

## 🎉 完了！

これで準備完了です。LINEやDMでの返信に困ったら、このアプリを使ってみてください！

