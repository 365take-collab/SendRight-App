# 🔧 登録できない場合のトラブルシューティング

## ✅ チェックリスト

### 1. サーバーが起動しているか確認

```bash
# ターミナルで確認
cd /Users/kawamuratakeshi/Cursor/sendright
npm run dev
```

**確認方法**:
- ターミナルに「Ready」と表示されているか
- http://localhost:3000 にアクセスできるか

### 2. ブラウザのコンソールを確認

1. ブラウザで http://localhost:3000/login を開く
2. **F12** または **Cmd+Option+I** で開発者ツールを開く
3. **Console** タブを選択
4. 登録を試して、エラーメッセージを確認

### 3. 入力内容を確認

- **メールアドレス**: 正しい形式か（例: `test@example.com`）
- **パスワード**: 6文字以上か

### 4. ネットワークタブを確認

1. 開発者ツールの **Network** タブを開く
2. 登録を試す
3. `/api/auth/register` のリクエストを確認
4. ステータスコードとレスポンスを確認

## 🔍 よくあるエラーと解決方法

### エラー1: "このメールアドレスは既に登録されています"

**原因**: 同じメールアドレスで既に登録されている

**解決方法**:
- 別のメールアドレスを使用
- または、サーバーを再起動してデータをリセット

### エラー2: "パスワードは6文字以上である必要があります"

**原因**: パスワードが短すぎる

**解決方法**: 6文字以上のパスワードを入力

### エラー3: "有効なメールアドレスを入力してください"

**原因**: メールアドレスの形式が正しくない

**解決方法**: 正しいメールアドレス形式（例: `user@example.com`）を入力

### エラー4: "ネットワークエラーが発生しました"

**原因**: サーバーが起動していない、またはネットワークの問題

**解決方法**:
1. サーバーが起動しているか確認
2. ターミナルで `npm run dev` を実行
3. ブラウザをリロード

### エラー5: エラーメッセージが表示されない

**原因**: JavaScriptエラーが発生している可能性

**解決方法**:
1. ブラウザのコンソール（F12）を確認
2. エラーメッセージを確認
3. ページをリロードして再試行

## 🛠️ デバッグ手順

### ステップ1: サーバーのログを確認

ターミナルでサーバーを起動している場合、エラーメッセージが表示されます。

### ステップ2: ブラウザのコンソールを確認

1. F12 で開発者ツールを開く
2. Consoleタブでエラーを確認
3. NetworkタブでAPIリクエストを確認

### ステップ3: 手動でAPIをテスト

ターミナルで以下を実行：

```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"test123"}'
```

正常な場合、トークンとユーザー情報が返ってきます。

## 💡 解決方法

### 方法1: サーバーを再起動

```bash
# 現在のサーバーを停止（Ctrl+C）
# 再度起動
cd /Users/kawamuratakeshi/Cursor/sendright
npm run dev
```

### 方法2: ブラウザのキャッシュをクリア

1. 開発者ツールを開く（F12）
2. Applicationタブ → Clear storage
3. ページをリロード

### 方法3: 別のブラウザで試す

- Chrome、Firefox、Safariなど、別のブラウザで試してみる

## 📞 まだ解決しない場合

以下の情報を確認してください：

1. **ブラウザのコンソールのエラーメッセージ**
2. **ターミナルのエラーメッセージ**
3. **NetworkタブのAPIレスポンス**
4. **入力したメールアドレスとパスワード**（個人情報は除く）

これらの情報があれば、問題を特定しやすくなります。












