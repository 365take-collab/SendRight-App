# 🔧 トラブルシューティングガイド

## ❌ 「ユーザーが見つかりません」エラー

### 原因
現在の実装では、ユーザーデータをメモリ内に保存しているため、以下の場合にデータが消えます：
- サーバーを再起動した時
- コードを変更してホットリロードが発生した時
- 開発サーバーが自動的に再起動した時

### 解決方法

#### 方法1: 再度アカウントを作成（推奨）

1. ブラウザで http://localhost:3000/login を開く
2. 「新規登録」タブを選択
3. メールアドレスとパスワードを入力してアカウントを作成
4. 再度ログイン

#### 方法2: ブラウザのキャッシュをクリア

1. ブラウザの開発者ツールを開く（F12 または Cmd+Option+I）
2. 「Application」タブを選択
3. 「Local Storage」→「http://localhost:3000」を選択
4. 「token」を削除
5. ページをリロードして再度ログイン

#### 方法3: サーバーを再起動

```bash
# 現在のサーバーを停止（Ctrl+C）
# 再度起動
cd /Users/kawamuratakeshi/Cursor/sendright
npm run dev
```

## 🔄 よくある問題と解決方法

### 問題1: ログインできない

**原因**: サーバー再起動でデータが消えた

**解決方法**:
- 再度アカウントを作成してください
- 開発環境では、サーバー再起動時にデータがリセットされます

### 問題2: 会員登録しても機能が使えない

**原因**: サブスクリプションが正しく登録されていない

**解決方法**:
1. 会員登録ページ（/subscribe）に再度アクセス
2. プランを選択して「今すぐ登録」をクリック
3. ホームページに戻って機能を試す

### 問題3: APIキーエラー

**エラーメッセージ**: "APIキーを確認してください" または "OPENAI_API_KEY is not defined"

**解決方法**:
1. `.env.local`ファイルを確認
2. `OPENAI_API_KEY=`の後に正しいAPIキーが設定されているか確認
3. サーバーを再起動（`.env.local`を変更した場合は必須）

### 問題4: 返信が生成されない

**原因**: 
- OpenAI APIキーが設定されていない
- クレジットが不足している
- ネットワークエラー

**解決方法**:
1. OpenAI APIキーが正しく設定されているか確認
2. OpenAIのBillingページでクレジット残高を確認
3. ブラウザのコンソールでエラーを確認（F12 → Console）

## 📝 開発環境の制限事項

### データの永続化
- **現在**: メモリ内に保存（サーバー再起動で消える）
- **本番環境**: データベース（PostgreSQL、MongoDB等）が必要

### 決済処理
- **現在**: デモ版（実際の決済なし）
- **本番環境**: Stripe等の決済サービスが必要

## 🚀 本番環境への移行

本番環境では以下の実装が必要です：

1. **データベースの実装**
   - PostgreSQL + Prisma（推奨）
   - または MongoDB、Supabase、Firebase

2. **決済処理の実装**
   - Stripe Japan の統合

3. **デプロイ**
   - Vercel（推奨）
   - または AWS、GCP

## 💡 ヒント

- 開発中は、サーバーを再起動するたびにアカウントを作り直す必要があります
- エラーが出た場合は、ブラウザのコンソール（F12）でエラーメッセージを確認してください
- 問題が解決しない場合は、サーバーを再起動してみてください












